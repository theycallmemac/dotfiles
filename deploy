for dot in .*
do
    if [ $dot == .git ];then
        true
    elif [ $dot == . ]; then
        true
    elif [ $dot == .. ]; then
        true
    elif [ $dot == README.md ];then
        true
    else
        cp -R $dot $HOME && echo "deployed $dot on local machine."
    fi
done

